<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">

  <!-- Background (optional, remove if transparent needed) -->
  <rect width="512" height="512" fill="#0f172a" rx="10"/>

  <!-- SAFE AREA (center 70-80%) -->
  <!-- Logo group -->
  <g transform="translate(256,256)">

    <!-- Example Logo (you can replace this part) -->
    
    <!-- Circle -->
    <circle cx="0" cy="0" r="130" fill="#1e40ff"/>

    <!-- Letter / Symbol -->
    <text x="0" y="60"
          text-anchor="middle"
          font-size="180"
          font-family="Arial, sans-serif"
          fill="white"
          font-weight="bold" stroke="white">
      X
    </text>

  </g>

</svg>